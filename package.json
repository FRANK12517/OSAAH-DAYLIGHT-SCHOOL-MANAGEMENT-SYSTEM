{
  "name": "osaah-daylight-school-management-system",
  "version": "0.1.0",
  "private": true,
  "description": "Phase 1 foundation for OsaaH Daylight School Management System",
  "type": "module",
  "scripts": {
    "assets:verify": "node scripts/verify-login-assets.mjs",
    "start": "node src/server.mjs",
    "test": "node --test",
    "test:relational": "node -e \"if (!process.env.OSAAH_TEST_DATABASE_URL) { console.error('OSAAH_TEST_DATABASE_URL is required for relational acceptance tests.'); process.exit(1); }\" && node --test test/integration/foundation-repositories.relational.test.js",
    "migration:status": "node scripts/migrate.mjs status",
    "migration:apply": "node scripts/migrate.mjs apply",
    "migration:validate": "node scripts/migrate.mjs validate"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "mysql2": "^3.24.4",
    "pdfkit": "^0.20.2"
  },
  "devDependencies": {
    "image-size": "^2.0.2"
  }
}
