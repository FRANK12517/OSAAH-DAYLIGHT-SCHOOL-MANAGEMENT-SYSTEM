import test from 'node:test';
import assert from 'node:assert/strict';
import http, { createServer as createHttpServer } from 'node:http';
import { createResultSlipPdfService, resultPdfFilename } from '../src/result-slip-pdf.js';
import { createApp } from '../src/server.mjs';

const baseResult=(overrides={})=>({resultType:'TERMINAL',studentId:'student-1',studentName:'Ama Mensah',studentIndexNumber:'OSAAH/2026/0001',classId:'JHS 2',academicYear:'2026/2027',term:'First Term',subjects:[{subjectName:'English Language',totalScore:84,grade:'1',subjectPosition:'1st',remark:'Excellent'}],totalScore:84,aggregate:1,classPosition:'1st',subjectsSat:1,average:84,assessment:{conduct:'Shows excellent conduct.',attitude:'Maintains a positive attitude.',interest:'Shows keen interest.',classTeacherRemarks:'Excellent progress.',headteacherRemarks:'Keep improving.'},attendance:{timesPresent:90,timesAbsent:2,totalSchoolDays:92},signatures:[{signatoryRole:'CLASS_TEACHER',name:'Teacher A',phone:'0241234567'},{signatoryRole:'HEADTEACHER',name:'Headteacher One',phone:'0241234569'}],...overrides});

test('server PDF generator creates branded A4 PDFs for JHS, Primary, KG, and Nursery terminal results',async()=>{const service=createResultSlipPdfService();for(const classId of ['JHS 2','Primary 1','KG1','Nursery 1']){const buffer=await service.pdf(baseResult({classId}));assert.match(buffer.subarray(0,8).toString(),/^%PDF-1\.[0-9]/);assert.ok(buffer.length>2500);}});

test('server PDF generator creates Mock PDFs only for eligible JHS result data',async()=>{const service=createResultSlipPdfService();for(const classId of ['JHS 1','JHS 2','JHS 3']){const buffer=await service.pdf(baseResult({resultType:'MOCK',mockLabel:'1st Mock',classId}));assert.match(buffer.subarray(0,8).toString(),/^%PDF-1\.[0-9]/);assert.ok(buffer.length>2500);}assert.match(resultPdfFilename(baseResult({resultType:'MOCK',mockLabel:'1st Mock',studentIndexNumber:'TEST/OSAAH J3 001'})),/^OSAAH_1st-Mock_Result_TEST-OSAAH-J3-001_/);});

test('PDF client controls and both result pages use real download endpoints without exposing edit controls',async()=>{const fs=await import('node:fs/promises');const terminal=await fs.readFile(new URL('../public/result-view.js',import.meta.url),'utf8');const mock=await fs.readFile(new URL('../public/mock-result-view.js',import.meta.url),'utf8');const helper=await fs.readFile(new URL('../public/result-pdf.js',import.meta.url),'utf8');const pages=(await fs.readFile(new URL('../public/results.html',import.meta.url),'utf8'))+(await fs.readFile(new URL('../public/mock-results.html',import.meta.url),'utf8'));for(const source of [terminal,mock]){assert.match(source,/EXPORT \/ DOWNLOAD PDF/);assert.match(source,/downloadResultPdf/);}assert.match(helper,/application\/pdf/);assert.match(helper,/URL\.createObjectURL/);assert.match(pages,/result-pdf\.js/);assert.doesNotMatch(helper,/window\.print/);});

test('PDF export routes are protected before any client-provided student lookup',async()=>{const server=createHttpServer(createApp());await new Promise(resolve=>server.listen(0,resolve));const port=server.address().port;const request=(path)=>new Promise((resolve,reject)=>{const req=http.get({port,path},res=>{res.resume();res.on('end',()=>resolve(res.statusCode));});req.on('error',reject);});assert.equal(await request('/api/academic/result/pdf?studentId=student-1&classId=Primary%201'),401);assert.equal(await request('/api/academic/mock-result/pdf?studentId=student-1&classId=JHS%203&mockLabel=1st%20Mock'),401);await new Promise(resolve=>server.close(resolve));});
