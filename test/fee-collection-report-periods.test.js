import test from 'node:test'; import assert from 'node:assert/strict'; import { createFeeCollectionsRepository } from '../src/fee-collections-repository.js';
test('collection reports apply canonical period and type filters', async () => { const rows=[{school_id:'s',collection_type:'EXTRA_CLASS',collection_date:'2026-09-07',amount_received_minor:10000,academic_year_id:'y1',term_id:'t1'},{school_id:'s',collection_type:'CANTEEN',collection_date:'2026-09-09',amount_received_minor:5000,academic_year_id:'y1',term_id:'t1'},{school_id:'s',collection_type:'CANTEEN',collection_date:'2026-10-01',amount_received_minor:7000,academic_year_id:'y2',term_id:'t2'}]; const adapter={query:async(sql,p)=>rows.filter(r=>r.school_id===p[0]),execute:async()=>({}),transaction:async fn=>fn(adapter)}; const repo=createFeeCollectionsRepository({adapter}); const a={id:'u',schoolId:'s',roleKey:'ACCOUNTANT_BURSAR',permissions:new Set(['*'])}; assert.equal((await repo.aggregateCollections({period:'WEEKLY',date:'2026-09-09'},a)).totalCollectedMinor,15000); assert.equal((await repo.aggregateCollections({period:'MONTHLY',date:'2026-09-09'},a)).count,2); assert.equal((await repo.aggregateCollections({period:'TERM',academic_year_id:'y1',term_id:'t1'},a)).count,2); assert.equal((await repo.aggregateCollections({period:'ACADEMIC_YEAR',academic_year_id:'y2'},a)).count,1); assert.equal((await repo.aggregateCollections({collection_type:'EXTRA_CLASS'},a)).totalCollectedMinor,10000); });
