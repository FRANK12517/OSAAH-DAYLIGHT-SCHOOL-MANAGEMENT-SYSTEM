import test from 'node:test';
import assert from 'node:assert/strict';
import { normalizeGhanaPhone } from '../src/ghana-phone.js';
import { createCommunicationService } from '../src/communication.js';

test('normalizes Ghana phone formats and preserves multiple child notifications', () => { assert.equal(normalizeGhanaPhone('024 123 4567'), '+233241234567'); assert.equal(normalizeGhanaPhone('233241234567'), '+233241234567'); assert.equal(normalizeGhanaPhone('+233241234567'), '+233241234567'); assert.equal(normalizeGhanaPhone('invalid'), null); const service = createCommunicationService(); const parent = { id: 'p1', portal: 'parent', children: [{ id: 's1', permanentStudentId: 'OSAAH/2026/0001' }, { id: 's2', permanentStudentId: 'OSAAH/2026/0002' }] }; service.notify({ recipientId: 'p1', studentId: 's1', permanentStudentId: 'OSAAH/2026/0001', studentName: 'Ama', title: 'A', body: 'One' }); service.notify({ recipientId: 'p1', studentId: 's2', permanentStudentId: 'OSAAH/2026/0002', studentName: 'Kojo', title: 'B', body: 'Two' }); assert.equal(service.listNotifications(parent).length, 2); service.markNotificationRead(service.listNotifications(parent)[0].id, parent); assert.equal(service.listNotifications(parent).filter((item) => item.status === 'UNREAD').length, 1); assert.equal(service.listNotifications({ id: 'p2', portal: 'parent', children: [{ id: 's3' }] }).length, 0); });
