import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';import {PROPRIETOR_SIDEBAR_ROUTES} from '../src/proprietor-sidebar-routes.js';test('public website follows protected login gallery and is not a sidebar route',async()=>{const html=await readFile(new URL('../public/index.html',import.meta.url),'utf8');assert.equal(PROPRIETOR_SIDEBAR_ROUTES.some(item=>item.moduleKey==='public-website'),false);assert.ok(html.indexOf('id="public-school-website"')>html.indexOf('11-community-panel.png'));for(const id of ['history','vision','mission','leadership','academics','admissions','facilities','gallery','news','contact'])assert.match(html,new RegExp(`id="${id}"`));assert.match(html,/data-portal="parent"/);assert.equal((html.match(/class="login-gallery-item"/g)??[]).length,11);});
