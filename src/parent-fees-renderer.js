export function escapeHtml(value) { return String(value ?? '').replace(/[&<>'"]/g, (c) => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' }[c])); }
export function renderParentFeeGroups(payload) { const groups = payload?.children ?? payload ?? []; return groups.map(({ student = {}, obligations = [] }) => `<article class="card"><h2>${escapeHtml(student.name || student.studentName || 'Child')}</h2><small>${escapeHtml(student.permanentStudentId || '')}</small><p>${obligations.length ? obligations.map((o) => `${escapeHtml(o.amountMinor ?? o.amount_minor ?? '')} · Payment allocation unavailable`).join('<br>') : 'No published fees for the selected period'}</p></article>`).join('') || '<p>No linked children.</p>'; }
