const form=document.querySelector('#result-context'),status=document.querySelector('#status'),host=document.querySelector('#result');let options={};const esc=(v)=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));async function api(url){const r=await fetch(url);const b=await r.json().catch(()=>({error:'Request failed.'}));if(!r.ok)throw Error(b.error||'Request failed.');return b;}async function load(){options=await api('/api/academic/options');form.elements.classId.innerHTML=options.classes.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('');form.elements.mockLabel.innerHTML=options.mockTypes.map(c=>`<option value="${esc(c)}">${esc(c)}</option>`).join('');syncStudents();}function syncStudents(){const list=options.students.filter(s=>s.classId===form.elements.classId.value);form.elements.studentId.innerHTML=list.map(s=>`<option value="${esc(s.id)}">${esc(s.indexNumber)} · ${esc(s.name)}</option>`).join('');}function render(x){const subjects=x.subjects||[];host.innerHTML=`<img class="result-header" src="${esc(x.headerAsset)}" alt="Osaah Daylight School Complex official result header"><h2>Mock Result Slip · ${esc(x.mockLabel)}</h2><p><strong>STUDENT NAME:</strong> ${esc(x.studentName||'—')}<br><strong>OSAAH STUDENT INDEX:</strong> ${esc(x.studentIndexNumber||'—')} · <strong>Class:</strong> ${esc(x.classId)}<br>${esc(x.academicYear)} · ${esc(x.term)}</p><div class="table-scroll"><table><thead><tr><th>Subject</th><th>Total Score</th><th>Grade</th><th>Subject Position</th><th>Remark</th></tr></thead><tbody>${subjects.map(s=>`<tr><td>${esc(s.subjectName||s.subjectId)}</td><td>${esc(s.totalScore)}</td><td>${esc(s.grade)}</td><td>${esc(s.subjectPosition||'—')}</td><td>${esc(s.remark)}</td></tr>`).join('')}</tbody></table></div><div class="slip-summary"><div><b>TOTAL SCORE</b>${esc(x.totalScore)}</div><div><b>AGGREGATE</b>${esc(x.aggregate??'N/A')}</div><div><b>CLASS POSITION</b>${esc(x.classPosition||x.position||'—')}</div><div><b>SUBJECTS SAT</b>${esc(x.subjectsSat??subjects.length)}</div><div><b>AVERAGE SCORE</b>${esc(x.average??0)}</div></div><h3>GES Teacher Assessment</h3><p>Conduct: Not recorded · Attitude: Not recorded · Interest: Not recorded</p><p>Class Teacher Remarks: Not recorded<br>Headteacher Remarks: Not recorded</p><h3>Attendance</h3><p>Not recorded</p><div class="signature-grid"><div>Class Teacher Name: ${esc(x.signatures?.find(s=>s.signatoryRole==='CLASS_TEACHER')?.name||'Name not configured')}<br>Phone: ${esc(x.signatures?.find(s=>s.signatoryRole==='CLASS_TEACHER')?.phone||'Phone not configured')}</div><div>Headteacher Name: ${esc(x.signatures?.find(s=>s.signatoryRole==='HEADTEACHER')?.name||'Name not configured')}<br>Phone: ${esc(x.signatures?.find(s=>s.signatoryRole==='HEADTEACHER')?.phone||'Phone not configured')}</div></div><button class="primary-button no-print" onclick="window.print()">Print / PDF</button>`;host.hidden=false;}form.elements.classId.addEventListener('change',syncStudents);form.addEventListener('submit',async e=>{e.preventDefault();status.textContent='Generating…';try{const q=new URLSearchParams(new FormData(form));render((await api('/api/academic/mock-result?'+q)).result);status.textContent='';}catch(e){status.textContent=e.message;host.hidden=true;}});load().catch(e=>status.textContent=e.message);
