const state = { portal: null };
const form = document.querySelector('#login-form');
document.querySelectorAll('[data-portal]').forEach((card) => card.addEventListener('click', () => { state.portal = card.dataset.portal; document.querySelector('.portal-grid').hidden = true; form.hidden = false; document.querySelector('#portal-label').textContent = `${state.portal.toUpperCase()} PORTAL SIGN IN`; document.querySelector('[name=username]').focus(); }));
document.querySelector('#back-button').addEventListener('click', () => { form.hidden = true; document.querySelector('.portal-grid').hidden = false; document.querySelector('#login-error').textContent = ''; });
form.addEventListener('submit', async (event) => { event.preventDefault(); const data = Object.fromEntries(new FormData(form)); data.portal = state.portal; const result = await fetch('/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }).then((response) => response.json()); if (result.error) { document.querySelector('#login-error').textContent = result.error; return; } await renderDashboard(result.user); });
async function renderDashboard(user) { document.querySelector('.login-brand').hidden = true; form.hidden = true; const dashboard = document.querySelector('#dashboard'); dashboard.hidden = false; const sidebar = await fetch('/api/sidebar').then((response) => response.json()); const children = user.portal === 'parent' ? await fetch('/api/parent/children').then((response) => response.json()) : null; dashboard.innerHTML = `<div class="app-shell"><aside class="sidebar"><div class="brand"><div class="logo-frame"><img src="/assets/osaah-daylight-logo.png" alt="Official school logo"></div><div><strong>OSAAH DAYLIGHT</strong><small>SCHOOL MANAGEMENT</small></div></div><nav>${sidebar.categories.map((group) => `<h4>${group.category}</h4>${group.modules.map((module) => `<a href="${module.route}">${module.icon} ${module.moduleName}</a>`).join('')}</nav>`).join('')}</aside><div class="workspace"><header class="topbar"><div><p class="eyebrow">${user.roleKey.replaceAll('_', ' ')} · ${user.portal.toUpperCase()} PORTAL</p><h2>${user.portal === 'parent' ? 'Parent Dashboard' : 'School Dashboard'}</h2></div><button id="logout" class="text-button">Log out</button></header><section class="hero"><p class="eyebrow">AUTHORIZED ACCESS</p><h3>${user.portal === 'parent' ? 'My Children' : 'Welcome to your school workspace'}</h3><p class="muted">${user.portal === 'parent' ? 'Select a child to view information you are authorized to access.' : 'Your modules are determined by your role and server-side permissions.'}</p>${children ? `<div class="children">${children.children.map((child) => `<article class="child-card"><strong>${child.name}</strong><span>${child.className}</span></article>`).join('')}</div>` : ''}</section></div></div>`; document.querySelector('#logout').addEventListener('click', async () => { await fetch('/api/auth/logout', { method: 'POST' }); location.reload(); }); }
