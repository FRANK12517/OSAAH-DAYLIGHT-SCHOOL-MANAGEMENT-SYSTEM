const state = { portal: null };
let dashboardBuildPromise = null;
const form = document.querySelector('#login-form');
const assistantModule = import('/ai-assistant.js');
const financialModule = import('/financial-intelligence.js');
const learningModule = import('/academic-attendance-intelligence.js');
const peopleModule = import('/admissions-workforce-intelligence.js');
const operationalModule = import('/operational-intelligence.js');
const executiveModule = import('/executive-intelligence.js');
const actionReviewModule = import('/ai-action-review.js');
const aiAdministrationModule = import('/ai-administration.js');
const assistantObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-ai-assistant')) assistantModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const financialObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-financial-intelligence')) financialModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const learningObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-learning-intelligence')) learningModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const peopleObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-people-intelligence')) peopleModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const operationalObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-operational-intelligence')) operationalModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const executiveObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-executive-intelligence')) executiveModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const actionReviewObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-ai-action-review')) actionReviewModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
const aiAdministrationObserver = new MutationObserver(() => { const workspace = document.querySelector('#dashboard .workspace'); if (workspace && !workspace.querySelector('#osaah-ai-administration')) aiAdministrationModule.then(({ mount }) => mount(workspace)).catch(() => {}); });
assistantObserver.observe(document.querySelector('#dashboard'), { childList: true });
financialObserver.observe(document.querySelector('#dashboard'), { childList: true });
learningObserver.observe(document.querySelector('#dashboard'), { childList: true });
peopleObserver.observe(document.querySelector('#dashboard'), { childList: true });
operationalObserver.observe(document.querySelector('#dashboard'), { childList: true });
executiveObserver.observe(document.querySelector('#dashboard'), { childList: true });
actionReviewObserver.observe(document.querySelector('#dashboard'), { childList: true });
aiAdministrationObserver.observe(document.querySelector('#dashboard'), { childList: true });
const escapeHtml = (value) => String(value ?? '').replace(/[&<>'"]/g, (character) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', "'": '&#39;', '"': '&quot;' }[character]));
if ('serviceWorker' in navigator) navigator.serviceWorker.register('/sw.js').catch(() => {});
const RETRY_KEY = 'osaah_retry_queue_v1';
function queuedRequests() { try { return JSON.parse(localStorage.getItem(RETRY_KEY) ?? '[]'); } catch { return []; } }
function saveQueuedRequests(queue) { localStorage.setItem(RETRY_KEY, JSON.stringify(queue.slice(-25))); }
async function flushRetryQueue() { if (!navigator.onLine) return; const remaining = []; for (const request of queuedRequests()) { try { const response = await fetch(request.url, { ...request.options, headers: { ...request.options.headers, 'X-Retry-Attempt': 'true' } }); if (!response.ok) remaining.push(request); } catch { remaining.push(request); } } saveQueuedRequests(remaining); }
window.queueServerRequest = async (url, options = {}) => { if (navigator.onLine) { try { const { retryable, ...safeOptions } = options; const response = await fetch(url, safeOptions); if (response.ok) return response; } catch {} } if (options.method && options.method !== 'GET' && options.method !== 'HEAD' && options.retryable === true) { const { retryable, ...safeOptions } = options; const queue = queuedRequests(); queue.push({ url, options: safeOptions, queuedAt: new Date().toISOString() }); saveQueuedRequests(queue); } throw new Error('Request queued for a later server retry'); };
window.addEventListener('online', flushRetryQueue); flushRetryQueue();
function updateConnectionStatus() { const status = document.querySelector('#connection-status'); if (status) status.textContent = navigator.onLine ? 'Online · changes sync with the school server' : 'Offline · viewing cached shell; data changes require the server'; }
window.addEventListener('online', updateConnectionStatus); window.addEventListener('offline', updateConnectionStatus); updateConnectionStatus();
function routeIsActive(route) { const current = window.location.pathname.replace(/\/$/, '') || '/'; const target = String(route).replace(/\/$/, '') || '/'; return current === target; }
function setActiveSidebarRoute(dashboard, route) { dashboard.querySelectorAll('.sidebar-link').forEach((link) => { const active = new URL(link.href, location.origin).pathname === route; link.classList.toggle('active', active); if (active) link.setAttribute('aria-current', 'page'); else link.removeAttribute('aria-current'); }); }
function renderWorkspaceRoute(dashboard, route, title) {
  const hero = dashboard.querySelector('.hero'); const host = dashboard.querySelector('#module-workspace'); const frame = dashboard.querySelector('#module-frame'); const status = dashboard.querySelector('#module-status');
  const navigationVersion = Number(dashboard.dataset.navigationVersion ?? 0) + 1; dashboard.dataset.navigationVersion = String(navigationVersion);
  setActiveSidebarRoute(dashboard, route);
  frame.onload = null; frame.onerror = null; frame.removeAttribute('src'); frame.hidden = true;
  if (route === '/') { hero.hidden = false; host.hidden = true; document.title = 'OsaaH Daylight School'; return; }
  hero.hidden = true; host.hidden = false; status.hidden = false; status.className = 'module-status'; status.textContent = `Loading ${title}…`; frame.title = title;
  const url = new URL(route, location.origin); url.searchParams.set('embedded', '1');
  frame.onload = () => { if (Number(dashboard.dataset.navigationVersion) !== navigationVersion) return; try { const body = frame.contentDocument?.body; if (!body || !body.textContent.trim() || /Authentication required/i.test(body.textContent)) throw new Error('The authorized module did not render.'); frame.hidden = false; status.hidden = true; document.title = `${title} | OsaaH Daylight`; } catch (error) { console.error('OSAAH module render failure', { route, error }); status.hidden = false; status.className = 'module-status error'; status.textContent = `Unable to open ${title}. Please retry or contact the system administrator.`; } };
  frame.onerror = (error) => { if (Number(dashboard.dataset.navigationVersion) !== navigationVersion) return; console.error('OSAAH module load failure', { route, error }); frame.hidden = true; status.hidden = false; status.className = 'module-status error'; status.textContent = `Unable to open ${title}. Please retry or contact the system administrator.`; };
  frame.src = url.pathname + url.search;
}
function navigateToRoute(dashboard, route, title, { replace = false } = {}) {
  const normalizedRoute = String(route || '/').replace(/\/$/, '') || '/';
  if (window.location.pathname !== normalizedRoute) {
    const state = { route: normalizedRoute };
    if (replace) history.replaceState(state, '', normalizedRoute);
    else history.pushState(state, '', normalizedRoute);
  }
  renderWorkspaceRoute(dashboard, normalizedRoute, title);
}
function renderNavItem(module, nested = false) { const href = module.moduleKey === 'logout' ? '#logout' : escapeHtml(module.route); const active = routeIsActive(module.route); const children = (module.children ?? []).map((child) => renderNavItem(child, true)).join(''); return `<a class="sidebar-link ${nested ? 'sidebar-child' : ''}${active ? ' active' : ''}" href="${href}" data-module="${escapeHtml(module.moduleKey)}"${active ? ' aria-current="page"' : ''} title="${escapeHtml(module.moduleName)}"><span class="sidebar-icon" aria-hidden="true">${escapeHtml(module.icon)}</span><span>${escapeHtml(module.moduleName)}</span></a>${children}`; }
function renderNavGroup(group, index) { const direct = group.category === 'DASHBOARD OVERVIEW' || group.category === 'LOGOUT'; if (direct) return `<div class="sidebar-direct">${group.modules.map((module) => renderNavItem(module)).join('')}</div>`; const active = group.modules.some((module) => routeIsActive(module.route) || (module.children ?? []).some((child) => routeIsActive(child.route))); const groupId = `sidebar-group-${index}`; return `<section class="sidebar-group${active ? ' is-active' : ''}"><button class="sidebar-group-toggle" type="button" aria-expanded="${active}" aria-controls="${groupId}"><span>${escapeHtml(group.category)}</span><span class="sidebar-chevron" aria-hidden="true">⌄</span></button><div class="sidebar-group-items" id="${groupId}"${active ? '' : ' hidden'}>${group.modules.map((module) => renderNavItem(module, true)).join('')}</div></section>`; }
document.querySelectorAll('[data-portal]').forEach((card) => card.addEventListener('click', () => { state.portal = card.dataset.portal; document.querySelector('.portal-grid').hidden = true; form.hidden = false; document.querySelector('#portal-label').textContent = `${state.portal.toUpperCase()} PORTAL SIGN IN`; document.querySelector('[name=username]').focus(); }));
document.querySelector('#back-button')?.addEventListener('click', () => { form.hidden = true; document.querySelector('.portal-grid').hidden = false; document.querySelector('#login-error').textContent = ''; });
const passwordInput = document.querySelector('#password-input');
const passwordToggle = document.querySelector('#password-toggle');
passwordToggle?.addEventListener('click', () => { const shouldShow = passwordInput.type === 'password'; passwordInput.type = shouldShow ? 'text' : 'password'; const label = shouldShow ? 'Hide password' : 'Show password'; passwordToggle.setAttribute('aria-label', label); passwordToggle.setAttribute('aria-pressed', String(shouldShow)); passwordToggle.title = label; passwordInput.focus({ preventScroll: true }); });
form.addEventListener('submit', async (event) => { event.preventDefault(); const data = Object.fromEntries(new FormData(form)); data.portal = state.portal; const submit = form.querySelector('button[type="submit"]'); submit.disabled = true; submit.textContent = 'Signing in…'; document.querySelector('#login-error').textContent = ''; try { const response = await fetch('/api/auth/login', { method: 'POST', credentials: 'same-origin', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(data) }); const result = await response.json(); if (!response.ok || result.error) { document.querySelector('#login-error').textContent = 'Incorrect username or password.'; return; } await renderDashboard(result.user, result.redirectTo); } finally { submit.disabled = false; submit.textContent = 'Sign in securely'; } });
restoreSession();
async function restoreSession() { try { const response = await fetch('/api/auth/session', { credentials: 'same-origin', headers: { Accept: 'application/json' } }); if (!response.ok) return; const result = await response.json(); if (result.user) { document.querySelector('.portal-grid').hidden = true; await renderDashboard(result.user, result.redirectTo); } } catch {} }
function renderDashboard(user) {
  if (dashboardBuildPromise) return dashboardBuildPromise;
  dashboardBuildPromise = (async () => { document.querySelector('.login-brand').hidden = true; form.hidden = true; document.querySelector('#login-gallery').hidden = true; document.querySelector('main.login-page').classList.add('dashboard-active'); const dashboard = document.querySelector('#dashboard'); dashboard.hidden = false; if (dashboard.querySelector('.app-shell')) return; const sidebar = await fetch('/api/sidebar').then((response) => response.json()); const children = user.portal === 'parent' ? await fetch('/api/parent/children').then((response) => response.json()) : null; if (dashboard.querySelector('.app-shell')) return; dashboard.innerHTML = `<div class="app-shell"><button class="sidebar-menu-button" type="button" aria-expanded="false" aria-controls="primary-sidebar">☰ Menu</button><div class="sidebar-backdrop" hidden></div><aside class="sidebar" id="primary-sidebar" aria-label="Primary navigation"><div class="brand"><div class="logo-frame"><img src="/assets/osaah-daylight-logo.png" alt="Official school logo"></div><div><strong>OSAAH DAYLIGHT</strong><small>SCHOOL MANAGEMENT</small></div></div><nav>${sidebar.categories.map(renderNavGroup).join('')}</nav></aside><div class="workspace"><header class="topbar"><div><p class="eyebrow">${escapeHtml(user.roleKey.replaceAll('_', ' '))} · ${escapeHtml(user.portal.toUpperCase())} PORTAL</p><h2>${user.portal === 'parent' ? 'Parent Dashboard' : user.roleKey === 'PROPRIETOR' ? 'Proprietor Dashboard' : 'School Dashboard'}</h2></div><button id="logout" class="text-button">Log out</button></div><section class="hero"><p class="eyebrow">AUTHORIZED ACCESS</p><h3>${user.portal === 'parent' ? 'My Children' : 'Welcome to your school workspace'}</h3><p class="muted">${user.portal === 'parent' ? 'Switch between your children to view authorized information.' : 'Your modules are determined by your role and server-side permissions.'}</p>${children ? `<label class="child-switcher">Selected child<select id="child-select">${children.children.map((child) => `<option value="${escapeHtml(child.id)}">${escapeHtml(child.name)} · ${escapeHtml(child.className)}</option>`).join('')}</select></label><div class="parent-card-grid">${(sidebar.parentCards ?? []).map((card) => `<a class="parent-card" href="${escapeHtml(card.route)}"><strong>${escapeHtml(card.moduleName)}</strong><span>View authorized details</span></a>`).join('')}</div>` : ''}</section><section id="module-workspace" class="module-workspace" aria-live="polite" hidden><p id="module-status" class="module-status">Loading module…</p><iframe id="module-frame" class="module-frame" title="School module" hidden></iframe></section></div></div>`;
  const menuButton = dashboard.querySelector('.sidebar-menu-button'); const backdrop = dashboard.querySelector('.sidebar-backdrop'); const shell = dashboard.querySelector('.app-shell'); const setDrawer = (open) => { shell.classList.toggle('sidebar-open', open); menuButton.setAttribute('aria-expanded', String(open)); backdrop.hidden = !open; };
  menuButton.addEventListener('click', () => setDrawer(!shell.classList.contains('sidebar-open'))); backdrop.addEventListener('click', () => setDrawer(false));
  dashboard.querySelectorAll('.sidebar-group-toggle').forEach((button) => button.addEventListener('click', () => { const open = button.getAttribute('aria-expanded') === 'true'; button.setAttribute('aria-expanded', String(!open)); document.getElementById(button.getAttribute('aria-controls')).hidden = open; }));
  dashboard.querySelectorAll('.sidebar-link:not([data-module="logout"])').forEach((link) => link.addEventListener('click', (event) => { setDrawer(false); if (user.portal !== 'school') return; event.preventDefault(); const route = new URL(link.href, location.origin).pathname; navigateToRoute(dashboard, route, link.title || link.textContent.trim()); }));
  if (user.portal === 'school') { const renderCurrent = () => { const link = [...dashboard.querySelectorAll('.sidebar-link')].find((item) => new URL(item.href, location.origin).pathname === location.pathname); navigateToRoute(dashboard, location.pathname, link?.title || document.body.dataset.currentModule || 'School Module', { replace: true }); }; if (location.pathname !== '/') renderCurrent(); window.onpopstate = () => renderCurrent(); }
  document.querySelector('#logout').addEventListener('click', async () => { await fetch('/api/auth/logout', { method: 'POST' }); location.reload(); }); dashboard.querySelectorAll('[data-module="logout"]').forEach((link) => link.addEventListener('click', async (event) => { event.preventDefault(); await fetch('/api/auth/logout', { method: 'POST' }); location.reload(); })); })();
  return dashboardBuildPromise;
}
