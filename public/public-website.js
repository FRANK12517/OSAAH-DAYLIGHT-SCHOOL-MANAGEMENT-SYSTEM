const site=document.querySelector('#public-school-website');if(site){const links=[...site.querySelectorAll('.public-navigation a')];links.forEach(link=>link.addEventListener('click',()=>{links.forEach(item=>item.removeAttribute('aria-current'));link.setAttribute('aria-current','location')}));site.querySelector('[data-public-portal]')?.addEventListener('click',()=>{document.querySelector('[data-portal="parent"]')?.click();window.scrollTo({top:0,behavior:'smooth'})});}
